import { Box } from '@mui/material';
import { NextPage } from 'next';
import { memo } from 'react';

const HomePage: NextPage = () => {
  return <Box>hello mate</Box>;
};

export default memo(HomePage);
